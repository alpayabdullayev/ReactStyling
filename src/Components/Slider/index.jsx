import React from 'react'
import style from "./index.module.scss"

function Slider() {
  return (
    <div>
        <div className={`${style.container} ${style.slider}`}>
            slider
        </div>
    </div>
  )
}

export default Slider
